name: snap_serial
version: '0.1'
summary: Serialtest # 79 char long summary
description: |
  Test serial port  
grade: devel # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots

apps:
  snap_serial:
    command: bin/snap_serial

parts:
  snap_serial:
    source: https://github.com/7ider5/snap_serial.git
    source-tag: v0.1
    plugin: python
    python-version: python3
